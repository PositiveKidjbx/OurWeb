# 01-01-PLAN：锚点与信息架构契约校准

## 目标
统一 section id、导航锚点、CTA 跳转目标，确保结构与交互契约一致。

## 输入
- `index.html`
- `main.js`
- `.planning/REQUIREMENTS.md`（STR-01/02/03, UIX-03）

## 实施步骤
1. 盘点 Header 导航项与页面 section id 的映射关系。
2. 统一锚点命名与目标区块，修复不一致项。
3. 校验 Hero CTA 与 Footer 联系入口的目标一致性。
4. 更新必要注释或文档说明（仅最小改动）。

## 验证清单（必须全部通过）
- 手工点击导航，所有锚点可到达正确区块。
- CTA 点击可到达 Contact 区块。
- 控制台无阻断级报错。

## 提交规则（强制）
- 仅当验证全部通过，才允许 `git commit`。
- 提交后立即 `git push` 到远端，避免冲突堆积。

## 完成定义
- STR-01/STR-02/STR-03 的结构映射问题关闭。
- 产生 1 个原子提交（只包含本计划改动）。
